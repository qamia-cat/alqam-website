import React from 'react';

import Layout from '../components/layout';

const NotFoundPage = () => {
    return (
        <Layout />
    );
};

export default NotFoundPage;

export const Head = () => <title>Not found - Qamia - AI & Quantum Solutions Now</title>;
